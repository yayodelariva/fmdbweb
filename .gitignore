# Node / build artifacts
node_modules/
*.log

# Local environment
.env

# Migration bundles (DB dumps + uploads — never commit, contain user data)
migration/

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# WordPress: only track our custom code under wp-content/.
# Note: use wp-content/* (not wp-content/) so we can re-include children.
wp-content/*
!wp-content/themes/
wp-content/themes/*
!wp-content/themes/fmdb-theme/
!wp-content/mu-plugins/
